---
title: Workflows
description: Pharmacy workflow reference
icon: IconPill
---

All workflows live under `src/workflows/pharmacy/` and are composed as
`pharmacy.*`:

- `batchReceive`, `cndnIssue`, `expiryAlerts`, `getSale`, `grnVerify`,
  `itemUpsert`, `partialClose`, `piBook`, `poCreate`, `reorderSuggest`,
  `returnAgainstBill`, `saleFromRx`, `stockCorrect`, `stockLedger`,
  `transfer`, `transferAccept`.
