---
title: Inventory
description: Placeholder package — inventory module is not yet implemented.
icon: IconPackages
---

`@aspen-os/inventory` is a not-started stub with no module, workflows, tables, or events.

<Callout type="warn">
  Do not depend on `@aspen-os/inventory` — `src/index.ts` is empty and it registers no platform
  module.
</Callout>

## Status

```json title="packages/inventory/package.json"
{
  "name": "@aspen-os/inventory"
}
```

```ts title="packages/inventory/src/index.ts"
(empty — no exports)
```

No domain model, bounded context, or events. Listed as a stub in
`.working-docs/bounded-contexts/stubs.md`. A future module would follow the
management-aligned shape (`CODING_CONVENTIONS.md`,
`.agents/skills/write-module/SKILL.md`).
