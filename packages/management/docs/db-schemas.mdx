---
title: DB Schemas
description: Control-plane and tenant tables declared by the Management Plane module.
icon: IconDatabase
---

The Management Plane declares 3 control-plane tables (`tenant`, `service_provider`, `service_provider_user`). `tenant_schemas` is empty — no tables are pushed to tenant DBs.

## Enums

| Enum                      | Table Column              | Values                                         |
| ------------------------- | ------------------------- | ---------------------------------------------- |
| `tenant_status`           | `tenant.status`           | `onboarding`, `active`, `suspended`, `churned` |
| `service_provider_status` | `service_provider.status` | `active`, `inactive`                           |

## Control-Plane Tables

### `tenant`

Companion to the better-auth `organization` row. Holds lifecycle, plan, SP assignment, and per-tenant DB connection params.

| Column                                                                                                       | Type                     | Description                      |
| ------------------------------------------------------------------------------------------------------------ | ------------------------ | -------------------------------- |
| `id`                                                                                                         | text (PK)                | Shares the better-auth org ID.   |
| `status`                                                                                                     | tenant_status            | Default `onboarding`.            |
| `plan`                                                                                                       | text                     | Subscription plan identifier.    |
| `service_provider_id`                                                                                        | text                     | Active SP assignment.            |
| `signup_at`                                                                                                  | timestamptz (notNull)    | When the tenant signed up.       |
| `suspended_at`                                                                                               | timestamptz              | Last suspension timestamp.       |
| `suspended_reason`                                                                                           | text                     | Suspension reason.               |
| `churned_at`                                                                                                 | timestamptz              | Churn timestamp.                 |
| `churn_reason`                                                                                               | text                     | Churn reason.                    |
| `database_host` / `database_name` / `database_user` / `database_password` / `database_port` / `database_ssl` | text / integer / boolean | Per-tenant DB connection params. |
| `created_at`                                                                                                 | timestamptz (notNull)    | Default `now()`.                 |
| `updated_at`                                                                                                 | timestamptz (notNull)    | Default `now()`.                 |

Indexed on `status`, `service_provider_id`, and `plan`.

### `service_provider`

An implementation/integration partner that does physical-world onboarding work.

| Column        | Type                    | Description                                        |
| ------------- | ----------------------- | -------------------------------------------------- |
| `id`          | text (PK)               | `uuidv7("id").primaryKey()` — generated at insert. |
| `name`        | text (notNull)          | Display name.                                      |
| `slug`        | text (notNull, unique)  | URL-safe identifier.                               |
| `logo`        | text                    | Logo URL or Storage reference.                     |
| `description` | text                    | Free-text description of services.                 |
| `email`       | text                    | Primary contact email.                             |
| `phone`       | text                    | Primary contact phone.                             |
| `website`     | text                    | Website URL.                                       |
| `address`     | text                    | Address (single text field).                       |
| `status`      | service_provider_status | Default `active`.                                  |
| `created_at`  | timestamptz (notNull)   | Default `now()`.                                   |
| `updated_at`  | timestamptz (notNull)   | Default `now()`.                                   |

Indexed on `status`.

### `service_provider_user`

Join row linking one platform user to one service provider. Carries the SP membership — there is no `spId` column on `user`.

| Column                | Type                  | Description                  |
| --------------------- | --------------------- | ---------------------------- |
| `id`                  | text (PK)             | `uuidv7("id").primaryKey()`. |
| `service_provider_id` | text (notNull)        | FK → `service_provider.id`.  |
| `user_id`             | text (notNull)        | FK → better-auth `user.id`.  |
| `created_at`          | timestamptz (notNull) | Default `now()`.             |
| `updated_at`          | timestamptz (notNull) | Default `now()`.             |

Unique on (`service_provider_id`, `user_id`) and on `user_id` (1:1 user→SP). Indexed on `service_provider_id`.

<Callout type="warn">
  `role = "sp_user"` requires a `service_provider_user` row; any other role must have none. Enforced
  in the user workflows, not by the database.
</Callout>

## Tenant Schemas

`tenant_schemas` is empty — the module pushes nothing to tenant DBs.

## Relationships

```text
tenant ──N:1──→ service_provider (via service_provider_id)
service_provider_user ──N:1──→ service_provider
service_provider_user ──N:1──→ user (better-auth, 1:1 user→SP via unique user_id)
organization (better-auth) ──1:1──→ tenant (companion, shared ID)
```

<Callout type="info">
  Per-tenant domain tables (organization, branch, task, dms_file, employee, HR tables, etc.) are NOT
  defined here — they live in each tenant's own database and are managed by their own modules.
</Callout>
