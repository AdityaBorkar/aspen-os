---
title: Workflows
description: Workflows exposed by the Organization module — none; branch workflows live in Masters.
icon: IconListCheck
---

The Organization module exposes no workflows. Access via `p.organization` yields no groups.

No workflow groups. Branch workflows live in `@aspen-os/masters` as `p.masters.orgBranches` (`p.masters.branches` deprecated alias).

Links: [Events](/docs/organization/events) · [DB Schemas](/docs/organization/db-schemas) · [Masters Workflows](/docs/masters/workflows)
