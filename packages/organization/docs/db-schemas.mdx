---
title: DB Schemas
description: Database tables and enums declared by the Organization module — none.
icon: IconDatabase
---

The Organization module declares no tables; `control_plane_schemas` and `tenant_schemas` are empty, with no shared columns.

## Enums

| Enum | Values |
| ---- | ------ |
| —    | —      |

No enums.

## Tables

No tables. Branch table `org_branch` lives in `@aspen-os/masters`.

## Relationships

```mermaid
flowchart TB
  Empty["No tables"]
```

No relationships — no foreign keys.
