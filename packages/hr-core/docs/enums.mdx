---
title: Enums
description: Enums for the hr-core module.
icon: IconNumber123
---

Enums are `pgEnum` values defined in `src/db-schemas/enums.ts`.

| Enum                       | Values                                                                                    |
| -------------------------- | ----------------------------------------------------------------------------------------- |
| `hr_employee_status`       | `active`, `inactive`, `left`, `suspended`                                                 |
| `hr_gender`                | `female`, `male`, `other`, `prefer_not_to_say`                                            |
| `hr_employment_type`       | `contract`, `freelance`, `intern`, `part_time`, `permanent`, `temporary`                  |
| `hr_lifecycle_task_status` | `completed`, `in_progress`, `pending`, `skipped`                                          |
| `hr_onboarding_status`     | `cancelled`, `completed`, `in_progress`, `pending`                                        |
| `hr_separation_status`     | `cancelled`, `completed`, `in_progress`, `pending`                                        |
| `hr_promotion_status`      | `approved`, `completed`, `pending`, `rejected`                                            |
| `hr_transfer_status`       | `approved`, `completed`, `pending`, `rejected`                                            |
| `hr_skill_proficiency`     | `advanced`, `beginner`, `expert`, `intermediate`                                          |
| `hr_exit_interview_status` | `cancelled`, `completed`, `scheduled`                                                     |
| `hr_full_and_final_status` | `approved`, `cancelled`, `draft`, `paid`, `pending`                                       |
| `hr_holiday_type`          | `company`, `optional`, `public`, `weekly_off`                                             |
| `hr_access_level`          | `full`, `manage`, `read_only`                                                             |
| `hr_permission_action`     | `approve`, `archive`, `create`, `delete`, `manage`, `publish`, `reject`, `update`, `view` |
| `hr_announcement_status`   | `archived`, `draft`, `published`, `scheduled`                                             |
| `hr_announcement_channel`  | `custom`, `general`, `hr`                                                                 |
| `hr_announcement_priority` | `important`, `normal`, `urgent`                                                           |
