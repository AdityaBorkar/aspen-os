---
title: Overview
description: Architecture, tenancy model, initialization, config, and module structure of the DMS module.
icon: IconInfoCircle
---

The DMS module is a unified document/files management system built on `@aspen-os/platform`. The class-first records system (triage, classes, versions) and the free-form filesystem (folders, paths) are consolidated onto **one `file` entity**: `dms_file` carries both the folder/path attributes of the filesystem and the class/triage/lifecycle attributes of the records system. `file` is the primary term.

All binary storage is delegated to the platform's `StorageUnit` under version-bound keys (`dms/{tenant}/{fileId}/v{n}/{name}`); DMS owns the indexing semantics — classes, fields, labels, metadata, versions, file views, contacts, sharing, search, retention, and the trash.

## Module

```ts title="src/module.ts"
export class Dms implements Module {
  static create(config?: DmsModuleConfig): Dms;
  readonly $name = "dms"; // proxy key: p.dms
  readonly $dependencies: readonly string[] = [];
}
```

The module declares no platform-module dependencies.

## Architecture

The module operates against the **tenant** database (all 14 tables are tenant schemas; there are no control-plane tables):

```text
TENANT DATABASE
  dms_file, dms_file_version, dms_folder
  dms_class, dms_class_field
  dms_label, dms_entity_label
  dms_file_view, dms_share, dms_public_link
  dms_contact, dms_legal_hold, dms_setting, dms_access_log
```

## Lifecycle

```
upload into folder ──────▶ active ───────────▶ trashed ──── retention/auto-purge ──▶ purged
upload to triage ──▶ triaged ──classify──▶ active           (legal hold blocks purge)
                                     active ──expiry──▶ expired ──▶ trashed  (restore reactivates)
```

- **Upload into a folder** → `active` immediately. **Upload without a folder (staged for classification)** → `triaged`; `classify` validates required class fields, applies the file-naming schema (metadata-only rename), assigns `docNumber`, and sets `active`.
- **Soft delete** (records delete or filesystem trash) → `status = trashed`, `deletedAt`/`deletedBy` set. Restore → `active`.
- **Expiry scanner** promotes past-due files to `expired` (retains class, fields, versions).
- **Purge** (auto-purge or admin `deletePermanently`) removes all version objects and rows, cascading labels, shares, public links, and holds. Legal holds block purge.

## What It Exposes

The module exposes workflow + service groups:

| Getter      | Methods                                                                                                                                                                                                                                         |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `files`     | `upload`, `uploadBulk`, `get`, `getById`, `download`, `update`, `rename`, `move`, `copy`, `getDownloadLink`, `listVersions`, `newVersion`, `revert`, `deleteVersion`, `classify`, `delete`, `restore`, `purge`, `addMetadata`, `removeMetadata` |
| `folders`   | `create`, `get`, `getById`, `list`, `rename`, `move`, `update`, `delete`, `restore`                                                                                                                                                             |
| `classes`   | `create`, `get`, `list`, `update`, `archive`, `addField`, `updateField`, `deactivateField`                                                                                                                                                      |
| `labels`    | `create`, `update`, `delete`, `list`, `apply`, `remove`, `listByLabel`                                                                                                                                                                          |
| `shares`    | grants: `create`, `get`, `update`, `remove`, `list`, `listByGrantee`, `resolveToken`, `listSharedWithMe` · public links: `createPublicLink`, `getPublicLink`, `updatePublicLink`, `revokePublicLink`, `listPublicLinks`, `resolvePublicLink`    |
| `trash`     | `list`, `restore`, `deletePermanently`, `empty`, `purgeExpired`                                                                                                                                                                                 |
| `fileViews` | `apply`, `create`, `update`, `delete`, `list`, `listByOwner`, `getDefault`, `setDefault`                                                                                                                                                        |
| `triage`    | `list`, `detail`, `classify`                                                                                                                                                                                                                    |
| `contacts`  | `create`, `get`, `list`, `update`, `remove`                                                                                                                                                                                                     |
| `holds`     | `list`, `place`, `release`                                                                                                                                                                                                                      |
| `settings`  | `get`, `set`                                                                                                                                                                                                                                    |
| `search`    | `search`, `quick`, `promoteToView` (records + filesystem search merged)                                                                                                                                                                         |
| `activity`  | `get`, `getClass`, `getFile`                                                                                                                                                                                                                    |
| `versions`  | `get`, `list`, `getCurrent`, `new`, `revert`, `delete`                                                                                                                                                                                          |
| `access`    | `checkPermission`, `getEffectivePermission`, `isOwner`, `logAccess`                                                                                                                                                                             |
| `archive`   | `createArchive`, `processArchiveJob`                                                                                                                                                                                                            |
| `paths`     | `checkNameUniqueness`, `computeFilePath`, `computeFolderPath`, `getBreadcrumbs`, `getDepth`, `getFilePath`, `getFolderPath`, `getSubtreeMaxDepth`, `resolvePath`, `wouldCreateCycle`                                                            |
| `storage`   | `computeArchiveKey`, `computeStorageKey`, `copy`, `exists`, `get`, `getSignedGetUrl`, `move`, `remove`, `upload`                                                                                                                                |

```ts
// Access via the platform proxy ($name = "dms")
const p = IsolatedTenantPlatform.create(config, [dms]);

// Filesystem upload lands active; triage upload is staged
await p.dms.folders.create({ input: { name: "Projects", ownerId: userId } });
await p.dms.files.upload({
  input: { name: "invoice.pdf", folderId, body, contentType: "application/pdf", ownerId: userId },
});

await p.dms.files.upload({ input: { name: "scan.pdf", body, contentType: "application/pdf", ownerId: userId } });
await p.dms.triage.classify({ id: fileId, input: { classId, fieldValues } });

await p.dms.fileViews.create({ input: { name: "Invoices", ownerId: userId, filters: [...] } });
```

## Initialization

`$initialize()` receives `db`, `auth`, `pubsub`, and `storage` units, stores `db`/`pubsub`, and binds the `StorageUnit` into the storage bridge:

```ts
$initialize(units: {
  db: DatabaseUnit;
  auth: AuthUnit;
  pubsub: PubSubUnit;
  storage: StorageUnit;
}): void;
```

`$prepareRuntime()` registers two scheduled pg-boss jobs with `ctx.audit` + the running DB:

| Job            | Topic             | Cron         | Purpose                                                                                                                          |
| -------------- | ----------------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| Expiry scanner | `dms:expiry-scan` | `5 0 * * *`  | Promotes active files past their `expiryDate` to `expired`                                                                       |
| Auto-purge     | `dms:auto-purge`  | `30 3 * * *` | Permanently deletes `trashed`/`expired` files past retention and trashed folders past `trashRetentionDays`, skipping legal holds |

`$cleanup()` unsubscribes and unschedules both.

## Config

`DmsModuleConfig` (all optional):

```ts
type DmsModuleConfig = {
  allowedContentTypes?: string[]; // [] = all types allowed
  defaultAutoPurgeEveryHours?: number; // 24
  defaultCompression?: CompressionOption; // { enabled: true, mode: "none" }
  defaultDownloadLinkExpiry?: number; // 3600 (s)
  defaultRetentionDays?: number; // 180
  maxDownloadLinkExpiry?: number; // 604800 (7d)
  maxFileSize?: number; // 5 GB
  maxNestingDepth?: number; // 20 (folders)
  maxVersions?: number; // 10
  trashRetentionDays?: number; // 30 (folder trash)
};
```

## Infrastructure

`$prepareInfra()` returns:

```ts
$prepareInfra(): ModuleInfra {
  return {
    auth: { acl },                      // file, class, contact, share, file_view, folder, label, ...
    db: { control_plane_schemas: {}, tenant_schemas: dmsTables }, // 14 tenant tables
    events,                             // dms:* topics
  };
}
```

## Key Invariants

- **Triage gate** — uploads without a folder are staged in Triage; classification is the only transition out of Triage. Uploads into a folder are active immediately.
- **Version-scoped keys** — storage keys embed `{fileId}/v{n}`, so renames, moves, and naming-schema application are metadata-only; no S3 object moves.
- **Expiry is a lifecycle state** — the expiry scanner promotes files to `expired`; restore reactivates.
- **Retention is class-based; legal hold overrides** — auto-purge skips held files; `deletePermanently` rejects them.
- **Admin-only destructive authority** — classes, settings, holds, shared file views, and permanent trash deletes are admin-managed.
