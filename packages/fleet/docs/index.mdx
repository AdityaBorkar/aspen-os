---
title: Fleet
description: Placeholder package — fleet module is not yet implemented.
icon: IconTruck
---

`@aspen-os/fleet` is a **not-started** stub. Its `src/index.ts` is empty and `package.json` has no exports, dependencies, or scripts.

This package is intended to provide a fleet management module, but no scope has been defined yet. See `docs/TODO.md` for planned work.
