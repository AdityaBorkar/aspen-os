---
title: Accounting
description: Placeholder package — accounting module is not yet implemented.
icon: IconChartPie
---

`@aspen-os/accounting` is a **not-started** stub. Its `src/index.ts` is empty and `package.json` has no exports, dependencies, or scripts.

This package is intended to provide a centralized accounting module (ledger, etc.), but no scope has been defined yet. See `docs/TODO.md` for the planned accounting work.
