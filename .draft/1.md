---

CONTEXT.md > Known Gaps

Respect the AGENTS.md single surfaces: dms for files, comms for delivery/inbox, notes for generic notes, calendar for reminders (incl. task reminders via the existing task bridge — no second bridge).
One workflow action per file; compose public groups in the module router (existing convention, keep it).
Keep Valibot for domain input validation; do not introduce new Zod-based domain validation.
Keep one workflow group per bounded concept; move custody, keep clinical guards in healthcare.
Preserve branch scoping, gapless numbering, audit-per-mutation, and the encounter-immutability gate verbatim.

Do not recreate drive / notifications / task_reminder parallels (explicitly banned in AGENTS.md).
Do not add an overloaded platform run(); tenancy stays class-time (Single/Shared/Isolated).
Do not replace Drizzle pushSchema() with migration files; do not add FKs where the design uses soft text references.
Do not commit .output/; do not hand-edit the better-auth schema; follow kebab-case filenames and the anti-slop lint rules.
Do not change clinical semantics to fit the split (return windows, refill caps, re-admin gates, four-eyes, maker-checker stay).

---

Move the below functionality into existing platform modules

7.1 comms — all out-of-band delivery + opt-outs + templates
Moves: operations.messaging-send / messaging-retry / messaging-optout, patients.log-communication / share-slip, records.share-print / share-whatsapp, residents.family-summary / send-family-summary / feedback / raise-alert, psych.alert-senior escalation path, appointment/recall notifications. Tables: healthcareMessageLog / healthcareMessageOptout (currently in db-schemas/records.ts) → comms.message + comms.preference; healthcare keeps only the clinical reference (patient/encounter ids in event data). Why: single delivery surface + sweeper outbox already exists; healthcare’s queue-message→audit-notify sequence in operations/messaging-send.ts is a second outbox (already factored through shared/messaging.ts — port the gate, not just the call sites). Keep similar: same input fields accepted by deprecated wrappers; same opt-out blocking error; healthcare.* events still emitted (then comms.* events additionally).

7.2 dms — all file custody, versions, shares, holds
Moves: records.docs-attach / docs-verify, diagnostics.radio-report-attach, appointments.issue-certificate binaries, dental.consent scans, psych/caregiver consent scans. Pattern: healthcare stores clinical metadata (patient, encounter, type, verified-by) and a dms.file id instead of a raw file_path string; verification workflow stays clinical (four-eyes) but version/hold/share enforcement comes from dms.version/share/hold/public-link. Why: single file surface; retention holds and trash discipline must be shared. Do not create a parallel tag/share/trash model.

7.3 calendar — slots, occupancy, recalls, reminders
Moves: slot computation (appointments.compute-slots, practitioner conflict/next-free-slot, facility overlap), occupancy bookings (appointments, dental chairs, rehab/ayush sittings, counselling/tele), recall rules + recall lists (admin recall-rules, appointments.issue-recall, patients.enrol-recall, psych.recall-list), video/tele scheduling metadata. Pattern: follow the existing tasks→calendar bridge precedent — healthcare publishes intent, calendar owns the event/reminder rows; healthcare keeps clash/consent/encounter guards (adopt shared/scheduling.ts + shared/psych-booking.ts as the interim contract). Why: single reminder surface (explicit AGENTS.md rule); today there are three recall lists and N slot engines.

7.4 tasks — order-derived work execution
Moves: nursing.tasks-from-orders execution + nursing.board state, encounters.place-order fulfilment tracking, diagnostics.queue worklists, dental.pending-jobs. Pattern: clinical order stays in healthcare; fulfilment becomes a tasks.task (project/status/collaboration/link) with the clinical ids attached; nursing board becomes a read view over tasks filtered by patient/encounter, keeping amber/red clinical triage display (literals already in shared/board-query.ts). Why: no second task/status/automation engine.

7.5 masters — value sets, catalogues, templates
Moves: operations.masters-upsert/get + seed-presets (healthcareMasterEntry), admin master-versions/templates, diagnostics.test-master-upsert/panel-create catalogue parts, services discount-rule value parts, pharmacy item catalogue parts. Pattern: clinical modules reference masters.entity domains instead of local domain/key/value tables. Why: one shared catalogue surface; local seeding (seed-presets.ts) belongs with the catalogue.

7.6 compliance — evidence, verification, retention, audit views
Moves: operations.compliance-evidence/list (healthcareComplianceEvidence), records.retention-check enforcement, records.registers-append/void/export verification aspects, admin.logs + operations.audit-query (thin views over the platform audit unit), psych involuntary-hook + controlled-Rx attestation, diagnostics QC/authorize attestation. Why: compliance already owns obligation/verification/document/audit/dashboard; healthcare should emit evidence, not own the evidence store.

7.7 hr-core / hr-attendance / hr-leave — the entire staff group
Map: staff.upsert-staff + create-role/delete-role/list-roles/disable-user → hr-core.employee/position/access; staff.roster-plan → hr-attendance.shift; staff.attendance-mark → hr-attendance.attendance; staff.leave-request/leave-decide → hr-leave (keep maker-checker); staff.payroll-export → HR payroll hook, same export shape. Keep: practitioners stays clinical; practitioners’ leave blocks read HR leave rather than duplicating it. Evidence of misplacement: all 8 mutating staff workflows publish OPERATIONS_EVENTS, not staff events (docs/events.mdx:117 Operations table lists staff triggers).

7.8 workspace (+ future reports) — explorer + report definitions
Moves: operations.explorer-query/grant/export-csv + workflow-steps/explorer-tables.ts 22-collection allow-list, operations.reports-define/get/list/run + healthcareReportDefinition, billing collection-report/dues-aging/gst-export rendering, diagnostics tat-report rendering. Pattern: healthcare keeps the computations (aging, TAT, dues) as query helpers; the shared surface owns grants, views, CSV export, and report-definition storage — including the misplaced healthcareExplorerGrant table from db-schemas/staff.ts. Why: every module needs filtered views; a per-module explorer with its own grant table fragments access control. reports is a placeholder today (no src/workflows/), so workspace is the interim owner.

7.9 management / masters org-branch — branch meaning
Clarify, don’t merge blindly: healthcare_branch is a subdomain-routing row (subdomain unique, helpers in shared/branch-lifecycle.ts), not the org tree. After unifying the two branch CRUDs, keep the routing row in hc-foundation but reference management/masters org-branch for org meaning; document the distinction where the bounded-context language section already warns about it.

7.10 notes — generic note semantics (small, explicit)
Moves: masking/policy aspects of records.notes-mask.ts and the plain-text note CRUD shape of nursing.record-note.ts align with notes.note (create/get/list/update/delete). Stays: all clinical gating (encounter gate, EWS/IO/pain/risk content, witnessed drug admin, handover signatures, psych masking rules). Healthcare keeps the clinical note tables; notes owns the generic policy primitive if/when a shared masking/retention rule is needed. Do not move clinical content into notes.
