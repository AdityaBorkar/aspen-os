# TODO

merge payment_method and bank_account
remove the `masters.bank-account.primary.set` workflow. There must not be any thing like making the bank account "primary"
remove the `masters.bank-account.activate` and `masters.bank-account.deactivate` workflows and amalgamate that logic into `masters.bank-account.update` workflow

- organization table vs master_entity table: field-for-field name,email,phone,website,industry,tax_id,registration_number,founded_date,locale,timezone,status + slug/logo/branding vs code/organization_id(text, no FK)/kind taxonomy. master_entity_type includes organization, so masters shadow-links orgs via string.

Merge all the views/saved filters into a single store as "filter_views" in the "masters" module.
- Tasks task_saved_view (filters/sort jsonb untyped, type list|board|calendar|timeline, is_shared/is_default, owner+project) — packages/tasks/src/db-schemas/saved-view.ts.
- Workspace workspace_view (domain TEXT, conditions:ViewCondition[], sort, group_by, access personal|global, is_default per owner+domain) + VIEW_DOMAIN = tasks:task,dms:file,notes:note,hr:employee,compliance:document,workspace:draft — claims tasks:task, overlapping tasks views. Execution needs host registerViewResolver(domain,resolver) (packages/workspace/src/runtime.ts), none registered in-repo; apply(id) throws without one.
- DMS dms_file_view (filters:FileViewCondition[], is_shared/is_default) — packages/dms/src/db-schemas/file-view.ts.
- Stale key: packages/workspace/src/auth.ts:21 resource filterView vs table/workflows view/workspace_view.

Currently there are three address shapes: organization.address:text vs branch.*address* columns vs master_address structured table. No shared Valibot schema (only isValidCountryCode shared). Merge all of them into a single address shape in `masters` module and use a reference to that everywhere elsewhere.
Also branch inlines address_line1/line2,city,state,postal,country,email,phone,manager(text),notes(text) instead of using master_address (entity_type=branch), master_contact (entity_type=branch), note(scope=branch). Fix it.

Merge: master_bank_account vs master_payment_method workflows and db_schema. Payment Methods shal store different types of payment modes like Bank Accounts, UPI Accounts, Cards, etc.

P1 — Reminders / notifications / delivery split across 4 owners
- Single reminder table is correctly calendar_reminder (target_type, remind_at, is_recurring, is_sent) + per-minute calendar:reminder-scan dispatcher + task bridge materializing 3 rows per assignee (due-1d,1h,0) from task:due_date_changed/deleted/status_changed (packages/calendar/src/services/task-bridge.ts:58-86,137-166).
- But: compliance Reminder Engine (4 crons: 0 8 * * * expiry-scan, 0 0 * * * status-transition, 0 9 * * * escalation, 0 9 * * 1 weekly-summary) is a second engine with its own topic names; workspace workspace_schedule(dashboard_id,cron) + workspace:schedule_due is a third cron/delivery path that bypasses comms (docs say “host renders/delivers”, silent drop if unsubscribed, no subscribe in comms/src/services/event-bridge.ts); HR hr:announcement-scheduler(* * * * *) + announcement:published{announcement,recipientUserIds} fan-out (chunks of 10) is a fourth broadcast; comms comms:message-sweeper(* * * * *) outbox (queued→sending→sent/failed, MAX_DELIVERY_ATTEMPTS=5) is the nominal single delivery owner but misses workspace:schedule_due and special-cases OTP via direct adapter.send with no notification row.

Enforce delete/cascade policy via events (extend task-bridge pattern): branch/entity/contact/file deleted → delete/suppress notes, addresses, reminders, shares, watches. Document orphan policy per scope tuple; add scopeType registry (<module>:<entity>) as a real exported const, not free text — notes.scope_type, calendar.sourceType, workspace.domain, comms.source_module must import it.

6. Rename for clarity: organization module → org-profile;
8. HR pinAnnouncement:boolean → pinned:boolean to avoid confusion with workspace_pin
5. Designate masters as Party kernel (entity, contact, address, bankAccount, paymentMethod, uom, connection) and thin organization to OrgProfile{tenantId, slug, branding, locale} + Branch{profileId, code, type, parent} that references (not duplicates) masters rows. Alternatively merge into one party module — but do not keep two writable name/email/phone/tax sources. Share one Valibot AddressSchema, one ContactSchema.
9. Reminders + delivery: calendar owns detection + calendar_reminder; comms owns all delivery (in-app + out-of-band). Route workspace:schedule_due through comms.notify (or rename to dashboard:digest_requested and bridge it like calendar:reminder_due). Merge compliance escalation/weekly-summary into calendar reminders or document why expiry-scan stays separate (different cadence vs same table?). Remove HR fan-out duplication — HR publishes one announcement:published, comms fans out.


----

P0 — Triple organization + tenant identity collision

- better-auth organization (packages/platform/src/server/db/schema/auth.ts:87: pgTable("organization", {id:text,name,slug,logo…}), control-plane).
- Organization-module organization (packages/organization/src/db-schemas/organization.ts:11: pgTable("organization", {id:uuidv7(),name,slug unique,status,branding…}), tenant).
- Management tenant (packages/management/src/db-schemas/*: pgTable("tenant",{status,plan,service_provider_id,database_*}), control-plane, id = better-auth org.id via workflows/tenants/onboard.ts:48-99).
Same Postgres table name, same JS key, three meanings (identity vs rich profile vs provisioning record). mergeSchemas in base-platform.ts:219-228 throws on duplicate JS keys — safe today only because management does not contribute organization key, but semantically unmaintainable and hostile to custom tenant modules.

- HR duplicates org concepts locally: department,designation,employmentType,employeeGrade (14 control-plane tables) + own hrUser/hrRole/hrPermission/branchAccess parallel to platform user/member and masters contact/entity (packages/hr/src/module.ts, db-schemas/index.ts:140-195: 14 control + 40 tenant = 54 tables).

P1 — Tags / watchers / pins / activity triplicated
- Notes note.tags:text[].default([]) + arrayOverlaps filter vs DMS dms_label + dms_entity_label normalized join on file|folder. Different mechanism, tag vs label naming, no shared helper.
- Tasks task_watcher(task_id,user_id) (no events/audit) vs workspace workspace_watch(item_id,item_type,user_id) (draft|view|dashboard only, emits watch_subscribed/unsubscribed + audit). Semantically same, no bridge.
- Workspace workspace_pin(item_type TEXT free-form, PIN_ITEM_TYPE=draft|view|dashboard|file_view|triage|class) references DMS types with no FK/import, while recent/watch schemas restrict to draft|view|dashboard only — pin is a superset, inconsistent.
- Activity: tasks Activity Log table vs DMS activity projected from audit_log vs compliance audit entry via AuditUnit vs platform audit_log. Three patterns for “who did what”.

P1 — Terminology collisions
HR implicit employee;
Connection = masters integration credential (api_key|oauth2|webhook…, credentialRef in kvStore) — easily confused with old “business relationship”; Workspace = personal surface (drafts/views/dashboards) vs tasks Project/Board;
Reminder Engine (compliance expiry scanner) vs Reminder (calendar);
Watch.subscribe (workspace follow) vs PubSubUnit.subscribe (pg-boss); View vs FilterView (stale ACL key).

P2 — HR + DMS are monoliths; stubs are referenced but absent
- HR: 10 groups (access,announcement,attendance,employee,leave,lifecycle,overtime,position,setup,shift), ~60 topics, 54 tables in one $name="hr". Internal reconciliation.ts:132-140 subscribes only to own separation_completed/transfer_approved. Should be hr-core + composable submodules or explicitly layered groups.
- DMS: 18 workflow groups, 14 tables in one $name="dms".
- Compliance bridge subscribes to fleet:vehicle_registered (package src/index.ts empty, package.json name-only), accounting:financial_year_started (package does not exist), hr:employee_onboarded/separated (HR actually emits lifecycle:onboarding_*/separation_*, employee:created — strings do not match), masters:contact_created (unverified producer). OBLIGATION_GENERATE="0 6 * * *" defined in packages/compliance/src/utils/constants.ts:135 but never schedule()d — dead cron.
- Calendar fallback raw SQL SELECT category FROM "task_status" (task-bridge.ts:126-132) breaks module encapsulation to handle pre-terminal-field publishers.

10. Tags/labels, watchers, pins, activity: one label service (normalized join, not text[]), one watch service (tasks watcher becomes targetType=task adapter), one pin/recent vocabulary (align PIN_ITEM_TYPE with strict enum or keep free-form with registry validation), one activity projection from AuditUnit.audit_log (deprecate tasks Activity Log table, keep DMS projection as reference impl).

11. Split HR into hr-core (employee, position, department/designation/employmentType) + hr-attendance (attendance, shift, overtime) + hr-leave + hr-lifecycle + hr-access + hr-announcement, or enforce documented internal layers with no cross-layer table reads (only workflows/events). Same lens on DMS (separate records (class/field/classify) from drive (file/folder/version/share/label/trash) if it keeps growing).

12. Publish a custom-module contract: versioned event schemas imported from producer (never redefined — delete TaskDueDateChangedEventSchema clone in calendar, import from tasks), targetType/domain/itemType registries with register* SPIs (generalize registerViewResolver, REMINDER_TARGET, PIN_ITEM_TYPE), createAdapter(type).send() pattern from comms for new channels, and ensureDefaults + rotateCredential(kvStore) pattern for secrets. Add write-module/SKILL.md checklist item: “solo-run healthCheck green, no new unsubscribedTopics without optional-consumer flag.”

----

Perform a review and tell me if all the modules are clearly divided by their roles and responsibilities. The aim is to create a modular, maintainable system that shall work with a group of modules or individual modules or custom modules, as required. Give me recommendations/suggestions how to improve the module division and overall architectural seam.

Graphical overview of schemas, workflow steps, workflows, permissions, events in each module (in docs)  
How duplication between packages can be reduced?
What is the better appraoch for module architecture?

Schema Validation handled just before DB insertion. Also making the errors much more visible.

- DB Schemas
  - Drizzle Migrations could not be performed (using pushSchema instead — see ADR-0004)
  - How to handle migrations?
  - Export utility functions like lastUpdatedAt(), uuidv7()
- ACL List
  - How to handle migrations?
- PubSub Events
  - How to handle migrations?
- Replace zod with valibot entirely everywhere
- Merging ./working-docs with ./docs to ensure a single source of truth
- Dynamic Loading of Modules (/settings/features) = Enable/Disable each module and their feature flags

---

Create a scope of work for the "hr" module to add the following features:

- Canvas like Users
- Custom Roles and RBAC Permissions
- Users with Branch-wise access controls

Analyze the packages and their workflows and identify opportunities where workflows are duplicated across packages. The aim is to simplify and make the package workflows as first principles that can be used across packages rather than duplicating logic. Suggest refactors to create/delete/update modules and their workflows.
Identify inconsistencies and anti-patterns

Common soft-delete columns for all modules

"masters" module":
  - "payment-method" and "bank-account"

Create a project - Vasundhara
  
.list() workflows must contain a standardized list of filters and view statements for fetching the list.

fleet??
inventory??
crm??
compliance??

---

Create a scope of work for the "crm" module

- deals
- crm

---

Create a scope of work for the "dms" module

- templates
- new files (document only for now)
  doc_issuance / file-management in DMS

---

Create a scope of work for the "fleet" module

### 4.1 Vehicle

- Vehicle master record for company-owned or managed vehicles.
- **Fields**:
  - License Plate, Make (Brand), Model, Odometer Value (Last).
  - Fuel Type, Fuel UOM.
  - Chassis No., Acquisition Date, Vehicle Value, Location.
  - **Employee** managing the vehicle.
  - Insurance details (insurer, policy number, expiry).
- **Vehicle Attributes**: Color, Wheels, Doors, Last Carbon Check.

### 4.2 Vehicle Log

- Log of trips and usage for each vehicle.
- **Fields**: vehicle, employee (driver), date, odometer start/end, distance traveled, fuel consumed, fuel cost, purpose of trip.
- Used for expense tracking and maintenance scheduling.

### 4.3 Vehicle Expense Claims

- Vehicle-related expenses (fuel, maintenance, tolls, parking) are submitted as Expense Claims (from Phase 2) with the vehicle reference.
- Expense reports can be filtered by vehicle for fleet cost analysis.

### 4.4 Fleet Reports

- Vehicle expense summary per vehicle.
- Fuel consumption trends.
- Maintenance schedule tracking.
- Cost-per-kilometer analysis.

---

Create a scope of work for the "inventory" module

- goods
- services
- price_lists

---
---

Create a scope of work for the "scheduler" module to add the following features:
- appointment and scheduling

---

Create a scope of work for the "reports" module

---

Create a scope of work for the "compliance" module

- India DPDP
- India Compliance
- GDPR
- HIPAA
- RTO

---

Create a scope of work for the "accounting" module

- Ledger
- Transactions
- Accounts
- Banking???
- Sales???
- Purchase???
- Vouchers???

---

Create a scope of work for the "pharmacy" module

---

## Not a Priority Modules

### Support Module

Support = No Account Code. Grant Access. -> Download Screen Control Module and then show the screen to the operator.

First, Partner Support: Name, Role=Account Manager, Phone Number
Second, Application Support: Name, Role=Account Manager, Phone Number
Second, Raise Ticket, Request Callback

Show a Notification - Account Manager is requesting access to the application. YES / NO.
If you have not requested for support, press NO.
If you press YES, the account manager shall access the application and respond accordingly.

### discussions Module

Create a scope of work for the "discussions" module to add the following features:

- Creating rooms for discussions between certain user groups
- Creating a forum where anyone can post a question/topic and a thread opens up for discussion
- Support replies and reactions for all messages
- Every message recieves a unique link that can be shared with others
- Support different types of attachments (images, videos, etc.)
- Support for polls (like discord)
- Support for search functionality within the room/forum
- Support to show indicator for unread messages (do not show count)

All content and messages are stored in MDX.

### org_chat Module

- Messenging between users of the application within the same organization
- WhatsApp like functionality for internal chatting

### analytics Module

-

### ai Module

- Chatbot
- AI agents
- Automation

### Compliance Module

- India DPDP
- India Compliance
- GDPR
- HIPAA

---

## Skills

Research and explore how to make the existing skills more effective and efficient.

## Linting Rules

Analyze the repository and suggest new oxlint rules that can be made to ensure following of Coding Conventions and better code guidelines for contributors. DO NOT WRITE CODE. Make a report suggesting the new rules.

- Enable no-console with allowlist + CLI override (fix the docs as any first).
- eslint/no-restricted-imports (0 hits) — still worth adding to enforce two conventions going forward:
  - zod only in RPC/env code
  - uuid from drizzle-orm/pg-core only in audit/db-schema.ts (the one sanctioned native-UUID column).
- Create a set of rules to make index.ts module files like @management/src/index.ts for module structuring and code organization.

## Not a Priority

- Analytics
- Chatbot
- Agents
- Automations
- `tools` directory must be not needed
- Write tests for the entire project.
